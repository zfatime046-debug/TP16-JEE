package ma.ens.tp8monitoring.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration du logging.
 * Le logging est configuré via application.properties.
 * Ce fichier peut être enrichi avec une config Logback personnalisée si besoin.
 */
@Configuration
public class LoggingConfig {
    // Configuration gérée dans application.properties
}
